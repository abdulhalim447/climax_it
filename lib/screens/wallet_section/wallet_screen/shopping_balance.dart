import 'package:flutter/material.dart';

class ShoppingBalance extends StatelessWidget {
  const ShoppingBalance({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
